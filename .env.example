#=================================#
#           GENERAL               #
#=================================#
# Application Configuration
# These settings define core application behavior and public-facing URLs
#
# The public URL where your app is hosted. Used for callbacks and public facing URLs
# Development: localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=localhost:3000


#=================================#
#           DATABASE              #
#=================================#
# PostgreSQL connection string for Drizzle ORM
# Format: postgresql://[user]:[password]@[host]:[port]/[db_name]
# Make sure to change the password in production!
DATABASE_URL="postgresql://postgres:password@localhost:5432/starter-kit"


#=================================#
#            AUTH                 #
#=================================#
# Authentication Configuration
# Security settings for user authentication and OAuth providers
#
#! WARNING: CRITICAL SECURITY SETTING                                      
# BETTER_AUTH_SECRET must be changed before deploying to production!
# Current value 'dev' is NOT SECURE and will compromise your application!
# 
# Generate a strong production secret using either:
# 1. https://generate-secret.vercel.app/32
# 2. Terminal command: `openssl rand -base64 32`
#
# Keep this secret private and never commit the production value!
BETTER_AUTH_SECRET=

# The base URL of your application for authentication callbacks
# Development: http://localhost:3000
# Production: https://your-domain.com
# Must match your deployment URL and include protocol (http/https)
BETTER_AUTH_URL=http://localhost:3000

# GitHub OAuth
# Get these from https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=


#=================================#
#            EMAIL                #
#=================================#
# Email Service Configuration
# SMTP settings for sending transactional emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_username
SMTP_PASSWORD=your_password
SMTP_FROM=noreply@yourdomain.com